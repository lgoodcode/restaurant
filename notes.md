# need to adjust css or the specials image container so that it retains a minimum
    height so it doesn't glitch when changing

# fix icons css

# get SASS

# fix colors on items

# need webpack to build the client js